category: Net
requires: mingw64-i686-glib2.0 mingw64-i686-runtime pkg-config
sdesc: "MMS stream protocol library"
ldesc: "LibMMS is a common library for parsing mms:// and mmsh:// type network streams.
 These are commonly used to stream Windows Media Video content over the web.
 LibMMS itself is only for receiving MMS stream, it doesn't handle sending at all."

